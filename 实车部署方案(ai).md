# 实车纯 Point-LIO 导航部署方案

> 目标：实车跑「Point-LIO 里程计 + 固定栅格图」的 Nav2 导航，导航速度直发底盘。
> 不启用重定位（small_gicp），不使用先验点云；只在本机运行，不换电脑。

---

## 0. 总原则

**先链路、后算法；每一步单独验收，验收不过不往下走。**

实车调试最贵的是时间，而这套系统里「数据不通」和「参数不对」的表现几乎一样（都是车不动）。
所以拆成 5 段，每段都有**当场能看见的验收现象**，任何一段卡住都能立刻定位。

### 明确不做（本轮边界）

| 项目 | 处理 |
|---|---|
| small_gicp 重定位链路 | **保留但不启用**，以后想用随时能开，不要拆 |
| 先验点云 `srm_site_01.pcd` | 不动 |
| 决策 / 视觉 / 自瞄 | 不接入，不启动 |
| Point-LIO 算法参数 | 不动（建图时已验证可用） |

---

## 1. 三条已定决策

### 1.1 通信协议：用老工程（srm_auto_sentry）的，一模一样

新工程（pb2025 官方版）的串口协议**整体弃用**，老工程整包搬过来。

**红线：以下 7 项一个字节都不许改**（这是与 C 板的约定）：

| 项目 | 值 |
|---|---|
| 帧起始字节 | **0xA5**（官方版是 0x5A） |
| 帧头结构 | 起始(1) + 长度(**2 字节**) + 序号(1) + CRC8(1) = 5 字节 |
| 速度报文编号 | **0x0302** |
| 速度报文内容 | vx, vy, wz + `is_recovering`(1 字节) + 校验(2 字节) |
| CRC 算法 | 老包自带的（不要换成官方版的） |
| 发送周期 | **5 ms**（约 200 Hz） |
| 串口参数 | `/dev/ttyACM0`、115200、flow=none、parity=none、stop=1 |

### 1.2 决策部分：拷接口包 + 不启动决策模块

- `rm_decision_interfaces`（76 KB，纯消息定义）一起拷进工作空间。
- 决策模块不启动即可 —— 没人发指令，报文里 `is_recovering` 自然为 0（正常巡逻）。
- **报文结构不用改，一个字节都不动**，比硬砍 20 处代码又快又安全。

### 1.3 起步位置：场地中间，车头朝地图 +x（图右）

`--map-to-odom 2.30 2.00 0.0`

（数值依据见第 3 节）

---

## 2. 现状实测数据（本次核查结论）

### 2.1 地图

| 项目 | 实测 |
|---|---|
| 图幅 | 173 × 138 px @ 0.05 m = **8.65 × 6.90 m**，原点 (-1.99, -1.43) |
| 像素构成 | 障碍 15.2%、**未知 48.2%**、已观测可通行 36.6% |
| 未知区分布 | **集中在右半张图**：x≥2.33 区域未知占 73.8%，x<2.33 区域未知占 22.8% |
| 图幅几何中心 | (2.335, 2.020) |
| 墙体包围盒中心 | (2.285, 1.970)，场地约 8.55 × 6.80 m |
| 最大已观测可通行连通域中心 | (2.11, 1.90)，面积仅 15.1 m² |

**起步点 (2.30, 2.00) 已实测落在「已观测可通行」像素上**（值 254），不是未知区、不是障碍。

### 2.2 关于 `free_thresh` 的决定：**保持 0.25 不变**

原先怀疑「48% 未知被当成可通行」是坑，实测并看图后**决定不改**，理由：

- pgm 里的 205（未知）在 `free_thresh: 0.25` 下被判为 free；实测跑过 map_server，
  加载结果是 **0% 未知 / 84.8% 自由 / 15.2% 障碍**。
- 若改成 0.196 让未知保持未知：全局代价地图会出现大片 NO_INFORMATION，而 ThetaStar 规划器
  **不穿越未知区**，右半张图（74% 未知）会直接变成不可规划区域，目标点落在那儿就失败。
- 保持现状则整图可规划，真实障碍由实时的 `terrain_map` 局部代价层负责补上。

**结论**：先按现状跑通。**但地图本身要在 RViz 里和真实场地对一遍**（见 6.1）。

### 2.3 车身与参数

| 项目 | 现状 | 处理 |
|---|---|---|
| `robot_radius` | 0.2 m | 车体圆柱半径 0.27 m、轮子外廓约 0.33 m → **改为 0.33**（保守，保证不蹭） |
| 速度上限 | 2.5 m/s（上游他车值） | **首跑降到 0.5 m/s**，跑通后再放 |
| `vehicleHeight` | 0.5 m | **不改**：查源码后确认它表示「地面以上多高算障碍物高度带」，不是雷达安装高度 |

---

## 3. 起步位置的计算依据

「中间」取地图图幅中心，三个独立算法结果一致：

| 算法 | 结果 |
|---|---|
| 图幅几何中心 | (2.335, 2.020) |
| 墙体包围盒中心 | (2.285, 1.970) |
| 最大可通行区域中心 | (2.310, 1.995) |

三者相差 < 5 cm，**取 (2.30, 2.00)**。

**yaw = 0**：车头对准地图 +x（即地图图像的**右侧**）。

原理：Point-LIO 起步瞬间 odom 系的朝向就是车头方向且 yaw=0，所以 map→odom 的 yaw 就等于
「车头方向在地图坐标系里的角度」。车头朝图右 = 朝 map +x = yaw 0，参数最干净。

**现场复核方法**：车停在场地中间、车头摆正朝图右，起系统后在 RViz 看车与地图是否贴合；
有偏差就微调 `--map-to-odom` 的三个数。

---

## 4. 五阶段实施

### 阶段 1：把雷达点亮（P0，必须最先做）

**做法**：`script/start_real_nav.sh` 里那条驱动命令，从「直接 ros2 run」改为
**指定节点名 + 传绝对路径的配置文件**。

原因（两条独立故障，任一都致命）：

1. 驱动代码里节点名是 `livox_driver_node`，参数文件顶层键是 `livox_ros_driver2`
   → 名字对不上，整段参数被**静默忽略**，`user_config_path` 停在默认 `path_default`。
2. `user_config_path` 的值写成 `$(find-pkg-share ...)/...`，这是 launch 专有替换，
   `ros2 run --params-file` **不做替换**。

**保持不动**：驱动外参必须全 0（网络参数照旧：雷达 192.168.1.184、主机 192.168.1.50）。
老工程那份带 `roll=-4°/yaw=-90°` 外参的配置**不能拿来用** —— 车体模型脚本有硬检查，非零会拒绝启动。

**现场前提**：给有线网卡配 `192.168.1.50/24`，并确认去雷达的路由走有线口
（本机 WiFi 也在 `192.168.1.0/24`，存在路由冲突风险）。

**验收**：
1. `ros2 param get /livox_driver_node user_config_path` → 绝对路径，**不是** `path_default`
2. `/livox/lidar` 类型为 `livox_ros_driver2/msg/CustomMsg`，约 10 Hz
3. `/livox/imu` 约 200 Hz

**回退**：这条路径改坏了不影响 `script/start_real_slam.sh`（另一条路径），可回去验证雷达硬件。

---

### 阶段 2：纯 LIO 定位链（核心改动）

**目标状态**：Point-LIO 提供里程计、固定栅格图提供规划依据、`map→odom` 为固定常数、
**不启动 small_gicp、不加载先验点云**。

**思路**：不新造模式，而是把现有那**一个**开关拆成两个：

- 谁提供里程计 → 新开关 `use_lio_odometry`
- 要不要 GICP 修正 → 沿用原 `use_pcd_localization`

新开关默认值跟随老开关，所以原三种模式行为完全不变，只是多出一个组合。

**改动点（4 个文件，约 13 处，都是同一个条件表达式）**：

| # | 文件 | 改什么 |
|---|---|---|
| 1 | `launch/bringup_launch.py` | 声明新开关；向 localization / navigation 两个子 launch 各透传一次 |
| 2 | `launch/localization_launch.py` | `point_lio` 启动条件：`use_pcd_localization` → `use_pcd_localization or use_lio_odometry` |
| 3 | `launch/navigation_launch.py` | **6 处**：`loam_interface` 条件（含组合节点分支）、里程计 TF 发布条件（2 份）、里程计 remap（2 份）、地面分割订阅点云的条件（2 份） |
| 4 | `script/start_real_nav.sh` | 新增 `--lio` 开关；三者互斥校验 |

**不用改的地方**：静态 `map→odom` 的条件本来就是「重定位关时才发」，纯 LIO 模式下重定位是关的
→ 自动生效。**顺带白捡**：`--map-to-odom` 在这个模式下自动可用。

**漏改的后果**：TF 不发（车在图上不动），或地面分割订阅了没人发布的话题（避障失效）。

**验收**：
1. `tf2_echo odom base_link` 有值；**静止时不漂**
2. `tf2_echo map base_link` 有值
3. `/odometry`、`/terrain_map` 都有数据
4. RViz 里车的位置与朝向和地图一致

**回退**：新开关默认关，不加 `--lio` 即回到当前行为。

---

### 阶段 3：底盘速度链

**做法**：
1. **挂起官方版同名包**：从编译排除，**并清除其已安装/已编译产物**（两件事都要做，
   否则会出现「编了新代码跑的是旧行为」这种最耗时间的现象）。
2. 老包**整包拷入，包名不变**（可执行名、话题名、launch 名全不用改）。
3. `rm_decision_interfaces` 拷入。
4. 决策模块不启动。

**三个必须注意的点（已核实）**：

- **参数能否生效取决于节点名** —— 和老雷达驱动是同一类坑，但后果更直接：
  老包节点在代码里叫 `StandardRobotPpRos2Node`，参数文件顶层键是 `/standard_robot_pp_ros2`，
  两者能对上**全靠 launch 里那行改名**。而老包的参数**默认值是空的**（设备名空串、波特率 0）。
  一旦用「直接运行」的方式起它，参数全部不生效，**串口根本打不开**，报错还很含糊。
  → **必须用它的 launch 启动**。
- 老 launch 日志设为 `output="log"`（不进终端），调试期建议临时改成屏幕输出。
- 老 launch **只起它自己一个节点**，不会连带拉 robot_state_publisher
  → **不会与 SRM 车身模型抢 TF**。串口库 API 与官方版一致，编译风险低。

**台架验收（轮子离地）**：
1. 设备节点存在、当前用户在串口权限组
2. 能收到 C 板**回包**（双向通）
3. 小速度（0.1 m/s）下轮子**转**
4. 轮子**转向与符号正确**

**回退**：台架阶段轮子离地，出问题不会撞车。

---

### 阶段 4：参数与起步对齐

| 项目 | 现状 | 改为 | 原因 |
|---|---|---|---|
| `robot_radius`（两处 costmap） | 0.2 | **0.33** | 车体半径 0.27、轮子外廓约 0.33，按 0.2 会蹭墙 |
| 速度上限 | 2.5 m/s | **0.5 m/s** | 上游他车数值，首跑求稳 |
| `--map-to-odom` | 默认 0 0 0 | **2.30 2.00 0.0** | 起步点 = 场地中间、车头朝图右 |

`free_thresh` **保持 0.25 不变**（理由见 2.2）。

---

### 阶段 5：联调与上车（严格按顺序，每级不过不往下走）

1. 网线 + 路由确认
2. 雷达单独点亮（阶段 1 验收）
3. 起纯 LIO 定位，确认 TF 与 RViz 位置（阶段 2 验收）
4. 起串口节点，手柄推小速度，确认车动弹力与方向（阶段 3 验收）
5. 最后让 Nav2 接管，先给 1 米内的小目标点

---

## 5. 风险与回退

| 风险 | 表现 | 应对 |
|---|---|---|
| C 板协议与本节点不符 | 完全无回包 | 台架先验；必要时只改报文定义，不动控制逻辑 |
| 阶段 2 漏改条件 | TF 不发 / 避障无数据 | 验收第 1、3 项专拦此问题 |
| 纯 LIO 漂移 | 图上位置越跑越偏 | 方案固有代价，短程验证无碍；长期上重定位（链路已保留） |
| 起步位置没对齐 | 车一开就偏 | `--map-to-odom` 微调 |
| 同名包冲突 | 编译报错或跑到旧代码 | 挂起 + 清产物，两步都做 |
| 地图右半张未知 | 该区域规划依赖实时感知 | 见 6.1，先在 RViz 与真实场地核对 |

---

## 6. 现场待办 / 待确认

### 6.1 地图与真实场地核对（重要）

实测显示**右半张图（x≥2.33）有 73.8% 是未知区**，且全图散落大量孤立的障碍散点。
需要上车后在 RViz 里把 `/map` 与真实场地对一遍：

- 若右半张图确实是场地的一部分且已扫过 → 地图需要重扫或修补
- 若右半张图是场地之外 → 属正常，保持现状即可（当前阈值下它可通行，不影响左侧使用）

### 6.2 现场一次性配置

- [ ] 有线网卡配 `192.168.1.50/24`，确认路由走有线口
- [ ] 串口设备 `/dev/ttyACM0` 权限（`dialout` 组或 udev 规则）
- [ ] 手柄设备 `/dev/input/js0`（若要用 `--joy`）

### 6.3 记录留档

- [ ] 首跑的 `tf2_echo odom base_link` 与 `map odom` 数值（判断漂移）
- [ ] `/cmd_vel` 是否有零速穿插（手柄与 Nav2 同时发的情况）

---

## 7. 附：本次核查确认「无需改动」的项

- nav2 插件：17 个 plugin 与 47 个行为树库**全部存在且已编译**，无缺失引用
- `use_sim_time`：参数文件内 15 处全为 `False`，实车传参不冲突
- 静态 TF 自查询（`base_frame == lidar_frame`）：实测返回单位变换，**不会卡死**
- 先验点云与栅格图**已在同一坐标系**（头部声明的旋转经逐点比对验证成立，误差 9.5e-7 m）
- `small_gicp` 源码**已在本地**（`build/.../_deps/small_gicp-src`，28 MB）；只要不删编译目录就不会联网

---

# 实施记录（本次已完成部分）

> 代码已改完并做了能离线做的全部验证。带硬件的验收留到现场。

## 阶段 1：雷达驱动启动方式 —— 已完成

**改动**：`script/start_real_nav.sh`
- 驱动命令补 `-r __node:=livox_ros_driver2`（让参数文件的 livox 段真正生效）
- 驱动命令补 `-p user_config_path:=<绝对路径>`（绕开 `$(find-pkg-share)` 不被展开的问题）
- 新增 `LIDAR_CONFIG_FILE` 变量（可用环境变量覆盖），并显式传给车体模型 launch，保证两边用同一份配置
- 新增**驱动外参自检**：非全零直接报错退出（避免点云被转两次这种隐蔽故障）

**离线验证**：
- `--check` 输出的驱动命令已包含上述两个参数
- 故意造一份 `yaw=-90` 的配置，脚本正确报错并拒绝启动

**现场验收（未做）**：`ros2 param get /livox_driver_node user_config_path` 应为绝对路径；`/livox/lidar` 为 CustomMsg @10Hz。

## 阶段 2：纯 LIO 开关 —— 已完成

**改动**：4 个文件
- `bringup_launch.py`：声明 `use_lio_odometry`，透传给两个子 launch
- `localization_launch.py`：point_lio 启动条件加上新开关（静态 map→odom 那处不用改，纯 LIO 下自动生效）
- `navigation_launch.py`：**10 处**同一个条件表达式统一加入新开关
- `script/start_real_nav.sh`：新增 `--lio`，三种定位方式改成互斥校验

**离线验证**（组合与非组合两条路都测了）：
- `point_lio`、`loam_interface`、`sensor_scan_generation`、`fake_vel_transform`、`terrain_analysis(_ext)` 全部启动
- `small_gicp_relocalization` **确认未启动**，先验点云未被加载
- `lifecycle_manager` 报 `Managed nodes are active`，代价地图等全部激活，无参数类型错误
- 静态 map→odom 实测输出 `translation: (2.300000, 2.000000, 0.000000)`，与设计一致

> **实测发现的注意事项**：组合模式（`use_composition:=True`，脚本默认）下，容器装载 13 个组件需要 **30～40 秒**。等待不足会以为「节点没起来」。现场启动后请等半分钟再看 RViz。

## 阶段 3：底盘速度链 —— 已完成（台架未做）

**改动**：
- 官方版串口包移入 `src/standard_robot_pp_ros2_official_SUPERSEDED/`，加 `COLCON_IGNORE` 挂起，并附说明文件
- 老工程串口节点整包拷入 `src/standard_robot_pp_ros2/`（**包名不变**）
- `rm_decision_interfaces` 拷入 `src/rm_decision_interfaces/`
- 给 `pb_rm_interfaces` **附加**两个消息定义（`ProjectileAllowance` / `SwitchPosition`，裁判系统数据），这样搬迁过来的节点代码**一行都不用改**
- 串口节点 launch 的日志从 `log` 改为 `screen`（调试期需要看到串口状态）
- `script/start_real_nav.sh` 新增第 3 步启动底盘节点，并提供 `--no-chassis` 关闭

**协议保真度验证（红线核对）**：搬迁后的文件与老工程原件 md5 **完全相同**：

| 文件 | 结果 |
|---|---|
| `include/packet_typedef.hpp` | md5 一致 |
| `src/crc_func.c` | md5 一致 |
| `src/standard_robot_pp_ros2.cpp` | md5 一致 |

全包比对只有一处差异：launch 里的日志输出（`log` → `screen`）。

**离线验证**：
- 编译通过（3 个包）
- 节点按 `/standard_robot_pp_ros2` 启动，参数**全部生效**：
  `device_name=/dev/ttyACM0`、`baud_rate=115200`、`flow=none`、`parity=none`、`stop=1`
- 无串口硬件时每 1 秒重试并打印 `Open serial port failed : No such file or directory`（现场插上 C 板后应转为正常收发）

**现场验收（未做）**：收到 C 板回包 → 小速度下轮子转 → 转向与符号正确。

## 阶段 4：参数 —— 已完成

| 项目 | 原值 | 新值 |
|---|---|---|
| `robot_radius`（两处 costmap） | 0.2 | **0.33** |
| `v_linear_max` / `v_linear_min` | ±2.5 | **±0.5** |
| `v_angular_max` / `v_angular_min` | ±3.0 | **±1.0** |
| `min_approach_linear_velocity` | 0.5 | **0.2** |
| `velocity_smoother.max_velocity` | [2.5,2.5,3.0] | **[0.5,0.5,1.0]** |
| `max_accel` / `max_decel` | ±4.5/±5.0 | **±1.0/±1.5** |
| `--map-to-odom` 默认值 | 0 0 0 | **2.30 2.00 0.0** |
| `free_thresh` | 0.25 | **不改**（理由见 2.2） |
| `vehicleHeight` | 0.5 | **不改**（是障碍物高度带，不是雷达高度） |

## 尚未验证、必须现场做的部分

1. **雷达真正出数据**（阶段 1 的三条验收）
2. **TF 链**：`tf2_echo odom base_link` 静止不漂、`tf2_echo map base_link` 有值
3. **底盘能动**：C 板回包 → 小速度 → 方向符号
4. **手柄**：`/dev/input/js0` 与权限
5. **地图与真实场地核对**（见 6.1）
6. **起步位姿微调**：车停场地中间朝图右，看 RViz 贴合度调整 `--map-to-odom`

## 现场一条命令

```bash
cd /home/srm/pb2025_sentry_ws
source install/setup.bash
bash script/start_real_nav.sh --lio
```

（要关 RViz 加 `--no-rviz`；要关底盘节点加 `--no-chassis`；加 `--check` 只打印不启动。）

---

## 现场调试记录（2026-09-11 晚，实车首跑）

### 已解决 —— 雷达无数据（根因：发现广播走错网卡）

现象：驱动进程活着、参数正确、`Init lds lidar success!`，但**没有任何话题**、数据端口 56301/56401 从未绑定。

根因：Livox 预编译 SDK 里**写死**用 `255.255.255.255` 做雷达发现广播，而本机
**同一网段有两张网卡顶着同一个 IP**（板载 `enp114s0` DOWN、USB `enx00e04c680635` UP），
内核把广播选到了 DOWN 的板载网口 → 包被丢弃 → SDK 发现不了雷达 → 不下发工作模式 → 没有数据。

```
ip route get 255.255.255.255 from 192.168.1.50
  修复前: dev enp114s0        ← 断开的网口, 包直接丢
  修复后: dev enx00e04c680635 ← USB 网卡
```

修复：`sudo ip addr del 192.168.1.50/24 dev enp114s0` + `sudo nmcli con delete mid360`。

**经验**：`ping` 通只证明单播路径没问题，**不代表广播能到**。雷达这类靠广播发现的设备
必须用 `ip route get 255.255.255.255 from <本机IP>` 单独验证广播出口。

### 已解决 —— 速度下发链路断开（根因：话题名不匹配）

搬迁过来的底盘节点订阅的是 **`/cmd_vel_chassis`**（SRM 老工程的命名），
而 pb2025 的 `fake_vel_transform` 默认输出到 **`/cmd_vel`** → 发布者 1、订阅者 0，链路断在这里。

修复（两处，都已改）：
1. `srm_nav2_params.yaml` → `fake_vel_transform.output_cmd_vel_topic: "cmd_vel_chassis"`
2. `start_real_nav.sh` → 手柄命令加 `joy_vel:=cmd_vel_chassis`（否则推杆车也不动）

**经验**：移植节点后要逐一核对**话题名**，不能只看消息类型；用
`ros2 node info <节点>` 看它到底订阅什么，再和发布端对齐。

### 已确认正常的部分

| 项 | 实测 |
|---|---|
| `/livox/lidar` | 有发布者，约 8~10 Hz |
| `/livox/imu` | **200 Hz** |
| `/aft_mapped_to_init` | 6.4 Hz（Point-LIO 出里程计） |
| `/odometry` | 4.6 Hz |
| `odom → base_link` | `Translation [0.015, 0.005, 0.000]`、z=0、roll/pitch=0 → **静止不漂** |
| 驱动参数 | `user_config_path` 绝对路径、`xfer_format=4`、`frame_id=livox_frame` 全部生效 |

### 待解决 —— 串口权限

`Open serial port failed : open: Permission denied`。
判断依据：`/serial/imu` 话题不存在 ⇒ 底盘节点一个 C 板回包都没收到。

修复：
```bash
sudo chmod 666 /dev/ttyACM0
sudo usermod -aG dialout $USER     # 永久, 需重新登录
```
